# FreshScan AI — Backend Dependencies
# Install with: pip install -r requirements.txt

-r requirements-base.txt

# ── Testing ───────────────────────────────────────────────────────────────────
# Used for running test_auth.py scripts locally (CI test deps are in requirements-ci.txt)
pytest>=8.0.0

# ── ML / Inference (optional — server starts without them in demo mode) ───────
# Comment these out if you don't have GPU/model files and just want demo mode.
torch>=2.2.0
torchvision>=0.27.0
slowapi==0.1.9  # rate limiting for FastAPI; added for per-user scan endpoint throttling

fastapi-cache2==0.2.2